{{ form_start(form) }}
    {{ form_row(form.content,{'attr':{'class':'form-control'},'label':'Blague'}) }}
    {{ form_row(form.answer,{'attr':{'class':'form-control'},'label':'Réponse'}) }}
    <button class="mt-4 btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}